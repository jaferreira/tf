var settings = {
    api: {
        hostUrl: 'localhost:3000',
        apiVersion: 'v1',
        apiBasePath: 'api'
    },
    mongo:{
        connString: 'mongodb://server/sportstats'
    }
};

module.exports = settings;